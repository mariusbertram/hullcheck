apiVersion: v1
kind: Service
metadata:
  name: {{ include "anchor-webui.fullname" . }}
  labels:
    {{- include "anchor-webui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "anchor-webui.selectorLabels" . | nindent 4 }}
