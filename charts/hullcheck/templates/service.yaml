apiVersion: v1
kind: Service
metadata:
  name: {{ include "hullcheck.fullname" . }}
  labels:
    {{- include "hullcheck.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hullcheck.selectorLabels" . | nindent 4 }}
