apiVersion: v1
kind: Service
metadata:
  name: hullcheck
  namespace: hullcheck
  labels:
    app.kubernetes.io/name: hullcheck
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: hullcheck
  ports:
    - name: http
      port: 8080
      targetPort: http
