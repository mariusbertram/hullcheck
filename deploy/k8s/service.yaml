apiVersion: v1
kind: Service
metadata:
  name: anchor-webui
  namespace: anchor-webui
  labels:
    app.kubernetes.io/name: anchor-webui
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: anchor-webui
  ports:
    - name: http
      port: 8080
      targetPort: http
