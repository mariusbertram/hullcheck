apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hullcheck-data
  namespace: hullcheck
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  # storageClassName: your-storage-class
