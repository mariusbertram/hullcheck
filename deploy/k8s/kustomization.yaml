apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: hullcheck
resources:
  - namespace.yaml
  - serviceaccount.yaml
  - configmap.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  # Uncomment the one matching your platform:
  # - route.yaml     # OpenShift
  # - ingress.yaml   # vanilla Kubernetes
  # Add your real pull secret separately, e.g.:
  # - pull-secret-example.yaml
